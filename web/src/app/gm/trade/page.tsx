import { PlaceholderPage } from "@/app/gm/_placeholder";

export default function TradeRoomPage() {
  return (
    <PlaceholderPage
      title="Trade Room"
      description="Evaluate trade scenarios side-by-side using your z-score engine. Compare the players you'd send vs. receive across all 16 categories."
    />
  );
}
