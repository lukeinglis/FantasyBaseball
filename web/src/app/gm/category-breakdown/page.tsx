import { PlaceholderPage } from "@/app/gm/_placeholder";

export default function CategoryBreakdownPage() {
  return (
    <PlaceholderPage
      title="Category Breakdown"
      description="Deep dive into each category — your weekly totals, pace vs. historical champions, and which players are contributing the most."
    />
  );
}
